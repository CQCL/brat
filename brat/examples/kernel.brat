# This shows a basic bell circuit

# Expectation:
#  - It should type check

# Reality:
#  - pba fails to parse (see juxt.brat)

zero :: Nat
zero = 9

H :: { a :: Qubit -o b :: Qubit}
H = ?und

Rz :: (th :: Float) -> {a::Qubit -o b::Qubit}
Rz = ?rz

pba :: Float -> {c :: Qubit,d::Qubit -o c::Qubit,d::Qubit}
pba = th -> { x, y -> (H, id)(x, y) }

th :: {Bool -> Bool}
th = ?th


