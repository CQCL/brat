# Expectation: This file typechecks
# Reality:
#  - id2 looks for `id` as a value instead of a computation

id :: Nat -> Nat
id = x -> x

id2 :: Nat, Nat -> Nat, Nat
id2 = x, y -> (id,id)(x, y)

id3 :: Nat, Nat -> Nat, Nat
id3 = x, y -> ?help
