open import lib.kernel (CX)

fanout(Vec(Nat, 3)) -> Nat, Nat, Nat
fanout = { [/\] }

swap(X :: $, Y :: $) -> { X, Y -o Y, X }
swap(_, _) = { x, y => y, x }

test(Vec(Qubit, 2)) -o Vec(Qubit, 2)
test = { [/\]; CX; [\/] }
