# Expectation: This file parses and typechecks
# Reality:
#  - id2 fails to typecheck as the types of the two id's are not properly merged

id :: Nat -> Nat
id = x -> x

id2 :: (x :: Nat), (y :: Nat) -> (x :: Nat), (y :: Nat)
id2 = id, id

id3 :: Nat, Nat -> Nat, Nat
id3 = x, y -> ?help
